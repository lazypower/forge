{{ctx.Locale.Tr "repo.projects.work_planning.mcp_provenance" .PrincipalName (DateUtils.TimeSince .CommittedAt)}}<br>
<span>{{ctx.Locale.Tr "repo.projects.work_planning.mcp_registered_client"}} <bdi>{{.RegisteredClientLabel}}</bdi>{{if .RegisteredInstallationLabel}} — <bdi>{{.RegisteredInstallationLabel}}</bdi>{{end}}</span><br>
{{if eq .ClientAttribution.Source "unavailable"}}<span class="work-runtime-attribution-unavailable">{{ctx.Locale.Tr "repo.projects.work_planning.mcp_runtime_attribution_unavailable"}}</span>{{else}}<span>{{ctx.Locale.Tr "repo.projects.work_planning.mcp_client_reported"}} <bdi>{{.ClientAttribution.Harness}}</bdi>{{if .ClientAttribution.HarnessVersion}} (<bdi>{{.ClientAttribution.HarnessVersion}}</bdi>){{end}}{{if .ClientAttribution.Model}} / <bdi>{{.ClientAttribution.Model}}</bdi>{{end}}</span>{{end}}<br>
<span class="text light">{{ctx.Locale.Tr "repo.projects.work_planning.mcp_attribution_notice"}}</span>
